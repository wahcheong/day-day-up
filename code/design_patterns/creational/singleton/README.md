# Singleton Design Pattern in C++

## 特点
- **线程安全**，利用静态局部变量（C++11及以上标准），无需额外锁机制，编译器会自动加锁
- **懒加载**，第一次调用getInstance()时才创建实例
- **禁止拷贝**，避免意外复制出多个实例
- **自动析构**，程序退出时自动调用析构（顺序不确定，但通常安全）

## 适用场景
- 全局配置管理器
- 日志系统
- 线程池（但需注意生命周期）

## 注意事项
- 避免过度使用单例（会引入全局状态，降低可测试性）
- 程序退出时析构顺序不确定，不要在析构中依赖其他单例
